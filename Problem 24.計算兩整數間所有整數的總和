#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    int n;
    scanf("%d", &n);
    int a, b;
    for(int i = 0; i < n; i++){
        scanf("%d %d", &a, &b);
        if(a>b) printf("%d\n", (a+b)*(a-b+1)/2);
        else printf("%d\n", (a+b)*(b-a+1)/2);
    }
	return 0;
}
