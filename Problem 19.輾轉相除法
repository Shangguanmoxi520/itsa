#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    int a, b;
    scanf("%d %d", &a, &b);
    while(a != 0 && b != 0){
        if(a > b) a -= b;
        else b -= a;
    }
    if(a) printf("%d\n", a);
    else printf("%d\n", b);
	return 0;
}
