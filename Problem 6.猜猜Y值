#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    int n,m;
    scanf("%d", &n);
    for(int i = 0; i < n; i++){
        scanf("%d", &m);
        if(m >= 50 && m <= 70) printf("%d\n", m);
        else printf("100\n");
    }
	return 0;
}
