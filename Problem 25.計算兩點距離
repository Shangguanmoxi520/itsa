#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(int argc, char *argv[]) {
    float x1,x2, y1, y2;
    scanf("%f %f %f %f", &x1, &y1, &x2, &y2);
    x1 = powf(x1-x2, 2);
    y1 = powf(y1-y2, 2);
    x1 = sqrt(x1+y1);
    printf("%.2f\n", x1);
	return 0;
}
