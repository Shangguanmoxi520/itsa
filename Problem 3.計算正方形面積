#include <stdio.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d", &n);
    double w;
    for(int i = 0; i < n; i++){
        scanf("%lf", &w);
        printf("%.1f\n", (int)(w*w*10+0.5)/10.0);
    }
	return 0;
}
